### 1. Setup: Generate Your Dataset

Since we cannot share external files easily, copy and run this code block first. It will create a Pandas DataFrame named `df` that serves as your dataset for all the questions.

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

# Seeding for reproducibility
np.random.seed(42)

# Generating synthetic data
dates = pd.date_range(start='2023-01-01', end='2023-12-31', freq='D')
categories = ['Electronics', 'Home', 'Clothing', 'Books']
regions = ['North', 'South', 'East', 'West']

data = {
    'OrderID': np.arange(1001, 1367),
    'Date': np.random.choice(dates, 366),
    'Category': np.random.choice(categories, 366),
    'Region': np.random.choice(regions, 366),
    'Price': np.random.uniform(10, 500, 366).round(2),
    'Quantity': np.random.randint(1, 10, 366),
    'Discount': np.random.choice([0, 0.05, 0.1, 0.2], 366, p=[0.6, 0.2, 0.1, 0.1])
}

# Introduce some null values for cleaning practice
data['Region'].flat[np.random.choice(366, 10, replace=False)] = np.nan
data['Price'].flat[np.random.choice(366, 5, replace=False)] = np.nan

df = pd.DataFrame(data)

# Display first few rows
print("Dataset Created Successfully!")
print(df.head())

```

---

### 2. The Challenge Questions

Here is your roadmap. Please solve these using **NumPy, Pandas, and Matplotlib**.

#### **Part I: Data Cleaning & Basics (Pandas focus)**

1. **Inspection:** Display the data types of all columns and check how many missing values (`NaN`) exist in `Region` and `Price`.
2. **Cleaning:**
* Fill missing `Region` values with the string "Unknown".
* Drop rows where `Price` is missing.


3. **Feature Engineering:** Create a new column called `Total_Sales`.
* Formula: : Total_Sales = Price x Quantity X(1 - Discount)



#### **Part II: Aggregation & Analysis (NumPy/Pandas focus)**

4. **Category Analysis:** Calculate the total revenue (`Total_Sales`) generated by each `Category`. Which category is the highest earner?
5. **NumPy Operations:** Using NumPy functions, calculate the **average** and **median** `Price` of items sold. Also, identify the standard deviation of the `Quantity` sold.
6. **Complex Filtering:** Filter the data to find all orders from the "North" region where the `Quantity` is greater than 5 and the `Discount` is 0 (no discount).

#### **Part III: Advanced Trends & Visualization (Matplotlib focus)**

7. **Time Series Trend:** Group the data by `Date` (specifically by Month) to see monthly sales trends.
* *Plot:* Create a **Line Chart** showing Total Sales per Month. Label your axes clearly.


8. **Regional Comparison:**
* *Plot:* Create a **Bar Chart** comparing the total number of orders (count of OrderID) across different `Regions`.


9. **Correlation Check:**
* *Plot:* Create a **Scatter Plot** to check if there is a relationship between `Price` and `Quantity`. (Do expensive items sell in lower quantities?)

Part IV: Data Merging & Relations (Joins Focus)

10.Identifying Returns (Inner Join): Perform an Inner Join between your main dataframe df and the returns_df.
Calculate the total monetary loss (sum of Total_Sales) caused by these returns.

11.Enriching the Dataset (Left Join): Perform a Left Join to merge returns_df into df.
The resulting dataframe should have all the original rows.
Create a new column called Order_Status.
Logic: If the Return_Reason is NaN (meaning it wasn't found in the returns table), set Order_Status to "Completed". Otherwise, set it to "Returned".
Hint: You might need to fill NaNs after the join.

Part V: Advanced Visualization (Matplotlib Focus)
12.Price Distribution (Histogram): We need to understand our pricing strategy.
Plot: Create a Histogram of the Price column.
Use 20 bins. Add a title "Distribution of Product Prices" and label the X-axis "Price ($)".

13.Market Share (Pie Chart): Visualise the contribution of each Region to the total revenue.
Plot: Create a Pie Chart showing the percentage of total Quantity sold by Region.
Ensure percentages are displayed on the chart (e.g., using autopct).

14.Outlier Detection (Box Plot): We want to spot expensive outliers in our categories
Plot: Create a Box Plot comparing the distribution of Price across the four different Categories.
Tip: This helps visualize the median, quartiles, and any extreme price points for each category.
